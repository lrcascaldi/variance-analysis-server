// ------------ IMPORT MODULES ---------------------
// imports mongoose
const mongoose = require("mongoose");

// ------------ SETTINGS ------------------
// settings

// ------------ MODELS ------------------
// company model
// company code, company name, company legal name, company legal id, company logo, address, city, state, country

// branch model
// company code, branch code, branch name, branch legal name, branch legal id, address, city, state, country

// business unit model
// business unit code, business unit name

// ------------ EXPORT MODULE ---------------------
// module.exports = ModuleName;
